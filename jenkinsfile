node{
   stage('BUILD'){
     def mvn = tool name: 'maven', type: 'maven'
     sh "mvn package"
     echo "BUILD IS SUCCESSFUL"
   }
   stage('DEPLOY'){
    sshagent(['jenkinstom']) {
    echo "deploy successful"
     }
    
    }
}  
